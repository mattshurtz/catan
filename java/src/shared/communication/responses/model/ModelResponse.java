package shared.communication.responses.model;

/**
 * Wrapper class representing the entire model of the game.  Contains all information about resources, game board state, player states, etc.
 * @author Alex&Nicky
 *
 */
public class ModelResponse {
	//TODO The data types in this model response are broken down into multiple classes.  Should I create a "response" version of each of these classes, 
	//or just use the classes that Garrett and Matt already made (in shared.model, and shared.model.map)?
}
